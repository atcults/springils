homeUrl: www.sanelib.com
welcome: Welcome to Sanelib

server:
  error: Internal server error occured. ^0

field:
  required: ~0 is required
  duplicate: ~0 with ~1 ^0 already exists.
  pattern: ~0 format is incorrect. Ex. ^0
  notExist: ~0 with ~1 ^0 does not exist.
  length: ~0 length should not more than ^0
  select: ~0 should be selected from list.

date:
  startDateAfterEndDate: ~0 should not be after end date.

holiday:
  startDateOutOfRange: ~0 should not be in past date. Today is ^0
  endDateOutOfRange: ~0 should not after fiscal year end date ^0